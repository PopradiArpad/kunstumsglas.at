The defaultMessages.json is generated from the react-intl messages during the build.
It's content is the current texts to i18nized.

The translatedMessages.json is only for data security, it's not used by the app.
The translatedMessages are fetched from the server and they can be changed on the cms
without recompiling.
